require 'spec_helper'

describe English::Number do
  it 'has a version number' do
    expect(English::Number::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
